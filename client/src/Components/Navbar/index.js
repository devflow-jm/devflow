import { connect } from "react-redux";
import Navbar from "./Navbar";
import _ from "lodash";

const s2p = state => {
  return {
    //
  };
};

const d2p = dispatch => {
  return {
    //
  };
};

export default connect(
  s2p,
  d2p
)(Navbar);
